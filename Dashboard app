import { BarChart3, CandlestickChart, Globe2, Landmark, Layers3, LineChart, Waves } from 'lucide-react'
import { dashboardCards } from './data/dashboard'
import { MetricCard } from './components/MetricCard'
import { TradingDecisionCard, TrendScoreCard } from './components/SignalCard'

const metrics = [
  { data: dashboardCards.spot, icon: <LineChart size={20} /> },
  { data: dashboardCards.futures, icon: <CandlestickChart size={20} /> },
  { data: dashboardCards.options, icon: <Layers3 size={20} /> },
  { data: dashboardCards.bankNifty, icon: <Landmark size={20} /> },
  { data: dashboardCards.vix, icon: <Waves size={20} /> },
  { data: dashboardCards.global, icon: <Globe2 size={20} /> },
]

export default function App() {
  return <main className="min-h-screen px-4 py-6 sm:px-6 lg:px-10 lg:py-9"><div className="mx-auto max-w-7xl"><header className="mb-8 flex flex-col justify-between gap-5 border-b border-slate-800 pb-6 sm:flex-row sm:items-end"><div><div className="mb-3 flex items-center gap-2 text-sky-300"><BarChart3 size={19} /><span className="text-xs font-bold uppercase tracking-[0.2em]">Market intelligence</span></div><h1 className="text-3xl font-semibold tracking-tight text-white sm:text-4xl">Nifty Momentum Dashboard</h1><p className="mt-2 max-w-xl text-sm leading-6 text-slate-400">A single view for spotting high-conviction Nifty trend days.</p></div><div className="rounded-xl border border-slate-800 bg-slate-900/60 px-4 py-3 text-sm text-slate-400"><span className="mr-2 inline-block size-2 rounded-full bg-amber-400" />Market data not connected</div></header><section className="grid grid-cols-1 gap-4 sm:grid-cols-2 xl:grid-cols-4">{metrics.map(({ data, icon }) => <MetricCard key={data.title} {...data} icon={icon} />)}<TrendScoreCard /><TradingDecisionCard /></section><footer className="mt-6 text-center text-xs text-slate-600">Dashboard skeleton · Live data modules will populate each signal independently.</footer></div></main>
}
